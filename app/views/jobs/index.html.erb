<h1>Result</h1>
<% if !flash[:notice].blank? %>
  <div class="alert alert-info">
  <%= flash[:notice] %>
  </div>
<% end %>
<br />
<%= link_to "New Joblist Submission", new_job_path, class: "btn btn-primary" %>
<br />
<br />
<!--
<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Download Link</th>
      <th>&nbsp;</th>
    </tr>
  </thead>
  <tbody>
    <% @jobs.each do |job| %>
      <tr>
        <td><%= job.name %></td>
        <td><%= link_to "Download Job", job.Joblist_url %></td>
        <td><%= button_to "Delete", job, method: :delete, class: "btn btn-danger", confirm: "Are you sure that you wish to delete #{job.name}?" %>
      </tr>
    <% end %>
  </tbody>
</table>
-->
